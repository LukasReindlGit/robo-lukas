"""
git-local module — read-only introspection of local git repositories.

No authentication, no browser, no network. Shells out to the system ``git`` binary.
Supports single repos and multi-repo sweeps via GIT_REPO_PATHS.
"""
