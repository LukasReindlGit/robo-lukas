# workflows

Composed pipelines that call **multiple modules** (scripts, Make targets, or small orchestrators — TBD).

## Examples (planned)

- **Morning briefing**: Jira assigned to me + To Do due today + git summary.
- **Ticket deep-dive**: Jira issue + related branch diff + last SF query template.

Add one file per workflow (`morning-brief.md` or `morning_brief.py`) as you implement them.
